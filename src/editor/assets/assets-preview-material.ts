export class AssetsPreviewMaterial {
    public constructor() {

        // let canvas = document.createElement('canvas');

        // canvas.getContext('2d')?.drawImage()
        // new canvas
        // new Image().
    }
}